{
	"name": "worker-scaping",
	"scripts": {
		"dev": "wrangler --port 3001 --persist-to ../worker-cache/.wrangler/state dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"axios": "^1.7.9",
		"cheerio": "^1.0.0",
		"hono": "^4.6.15",
		"winston": "^3.17.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20241230.0",
		"wrangler": "^3.96.0"
	}
}
